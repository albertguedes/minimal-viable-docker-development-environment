# PostgreSQL Configuration
POSTGRES_DB=dockerdb
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker

# Host networking (see compose.yaml) — services bind on loopback:
# HTTP 8080, PHP-FPM 9000, PostgreSQL 5432